import React from "react";

const WorkHistory = () => {
  return <div>
    <p>
        You currently do not have any work history associated with this Profile
    </p>
    <p>Click on the button to add a new work history</p>
  </div>;
};

export default WorkHistory;
